version: "3.9"

services:
  db:
    image:    environment:
    ports:
      - #DockerComposeDBPorts()
    volumes:
      - ./db:/docker-entrypoint-initdb.d

  platform:
    image: theharbormaster/banking-on-go-lang:latest
    ports:
      - "8081:8081"
    depends_on:
      - db
    links:
      - db

